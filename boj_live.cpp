#include <bits/stdc++.h>

using namespace std;
const int INF = 0x3f3f3f3f;
using pii = pair<int, int>;
using ll = long long;
using vi = vector<int>;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    int a;
    vi lis;
    cin >> a;
    vi v(a);
    for (int i = 0; i < a; ++i) {
        cin >> v[i];
    }


    return 0;
}
